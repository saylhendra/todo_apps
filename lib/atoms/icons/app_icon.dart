import 'package:flutter/material.dart';

import '../dimensions/app_size.dart';

class AppIcon {
  static Icon email = Icon(Icons.alternate_email, size: AppSize.m);
  static Icon logout = Icon(Icons.logout, size: AppSize.m);
}
